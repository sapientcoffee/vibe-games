description = "Instantly bootstrap a functional Tracer Bullet using high-velocity rules."
prompt = """
We are performing an espresso blitz. Our goal is a functional "Tracer Bullet" (UI -> Bridge -> Gemini 3).

### Phase 1: High-Velocity Scaffolding
Invoke a subagent to run the `scripts/setup_slice.sh` script. This script follows the **High-Velocity Setup Rules**:
1. **Force Public Index**: Uses `--index-url https://pypi.org/simple`.
2. **Explicit Paths**: Uses absolute repository paths for the bridge and virtual env.
3. **Hybrid Ready**: Scaffolds a bridge that can toggle between Gemini API and Vertex AI.

### Phase 2: Orchestration (Post-Scaffold)
Once scaffolding finishes, use the `tmux-orchestrator` skill to spawn:
- **Bridge Server**: `./skills/tmux-orchestrator/scripts/spawn.sh "app/.venv/bin/python3 bridge/main.py"`
- **Frontend UI**: `./skills/tmux-orchestrator/scripts/spawn.sh "cd frontend && npx serve ."`
- **Agent Logs**: `./skills/tmux-orchestrator/scripts/spawn.sh "tail -f playground.log"`

### Phase 3: Verification
Verify that Slice 0 is ready. Use `gemini-3-flash-preview` for the first verification turn.
"""
