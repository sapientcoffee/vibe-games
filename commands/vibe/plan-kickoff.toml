description = "Kickoff the planning and grilling session in a new Gemini pane."
prompt = """
You are the Orchestrator. The user wants to start the planning phase for: {{args}}.

### Action
Use the `tmux-orchestrator` skill to spawn a new Gemini session that immediately starts the planning process.

**Command to execute**:
`/home/robedwards/.gemini/extensions/vibe-games-extension/skills/tmux-orchestrator/scripts/spawn.sh "gemini /vibe:plan {{args}}"`

Once the pane is spawned, confirm the PANE_ID to the user and let them know the planning session is active.
"""
