[ ] if blueprint or context exits create a new file - this is so that I can add more capability after the initial run. The extension must deal wuth this.

[ ] simple way to stop/start servers - needs to find out what they are first but then makes it easier to restart and also output logs 

[ ] fix kanban board - 

Parse error on line 8:
...  issue-1([ADK-INIT] Scaffold Agent and 
-----------------------^
Expecting 'SPACELINE', 'NL', 'EOF', 'SHAPE_DATA', got 'SPACELIST'

```mermaid
kanban
  Todo

  In Progress

  Done

  issue-1([ADK-INIT] Scaffold Agent and Seed Data)
  issue-2([ADK-LOGIC] Implement stateless retrieval and filtering tool)
  issue-3([FRONTEND] Build Minimal UI with Markdown and Live Logs)
```
